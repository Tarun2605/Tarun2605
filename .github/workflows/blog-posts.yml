name: Latest Blog Posts

on:
  schedule:
    # Runs daily at 22:15 UTC (10:15 PM UTC)
    - cron: '15 22 * * *'
  workflow_dispatch:

jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - uses: actions/checkout@v4
      
      - uses: gautamkrishnar/blog-post-workflow@master
        with:
          feed_list: "https://dev.to/feed/tarun2605,https://medium.com/feed/@tarun2605"
          max_post_count: 5
          comment_tag_name: "BLOG"
          commit_message: "📝 Updated blog posts in README"
